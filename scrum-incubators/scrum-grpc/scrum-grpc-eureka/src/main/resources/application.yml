eureka:
  server:
    enable-self-preservation: false
  instance:
    appname: grpc-eureka-server
    hostname: localhost
  client:
    fetch-registry: false
    register-with-eureka: false
    service-url:
      defaultZone: http://localhost:8080/eureka/